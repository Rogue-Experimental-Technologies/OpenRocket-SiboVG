language: java
dist: bionic
sudo: false
addons:
  apt:
    packages:
      - ant
      - ant-optional

jdk:
  - openjdk11
  - openjdk17
  - oraclejdk11
  - oraclejdk17
 
script:
  - "ant -buildfile build.xml clean check jar unittest"

notifications:
    webhooks: https://www.travisbuddy.com/
